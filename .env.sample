# Frontend (Vite)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Backend (Node / Prisma)
DATABASE_URL=
# Optional: use Supabase connection pooler for serverless (recommended on Vercel)
# Example: postgresql://user:pass@aws-...pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1
POOLER_URL=
# Optional direct connection for migrations/Studio (non-pooler, port 5432)
DIRECT_URL=

# JWT
JWT_SECRET=

# Optional CORS origin override
CORS_ORIGIN=